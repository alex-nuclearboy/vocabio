Settings reference
==================

This page documents the project-specific helpers defined in
``config.settings``.

Environment variables, their defaults, and their purpose are documented in
:doc:`../configuration/environment`. PostgreSQL configuration and validation
rules are documented in :doc:`../configuration/database`.

The reference below is generated directly from the Python docstrings in the
settings module.

.. automodule:: config.settings
   :members: expand_database_url, get_str_env, build_database_config
